extends layout

block content
    h1= title

    ul
        for item in items
            a(href=item.url) #{item.name}
            p #{item.description}
    hr
    a(href=`${category.url}/delete`) Delete Category 