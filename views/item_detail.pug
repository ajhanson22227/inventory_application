extends layout

block content
    h1 #{title}
    <br />
    
    p $ #{item.price}
    p Currently #{item.stock} in stock
    <hr>
    a(href=`${item.url}/delete`) Delete <br>
    a(href=`${item.url}/update`) Update