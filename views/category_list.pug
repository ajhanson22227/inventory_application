extends layout

block content
    h1= title

    ul
        for category in category_list
            li
                a(href=category.url) #{category.name}
                p #{category.description}